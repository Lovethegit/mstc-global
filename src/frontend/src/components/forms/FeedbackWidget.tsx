// FeedbackWidget removed — rate button disabled site-wide.
// Backend feedback data and admin dashboard remain unaffected.
export function FeedbackWidget(_props: { pageName: string }) {
  return null;
}
